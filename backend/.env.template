# Copy this file to `.env` and fill in your real API key. Do NOT commit your `.env` file.
# Example for FAU HPC OpenAI-compatible endpoint
CUSTOM_OPENAI_URL=https://chat.hpc.fau.edu/openai/chat/completions
# Put your API key here (do not check into git)
CUSTOM_OPENAI_KEY=
# Optional: model to use for orchestration (gpt-4, gpt-5, etc.)
ORCHESTRATOR_MODEL=gpt-4
